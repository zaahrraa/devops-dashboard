apiVersion: v1
kind: Service
metadata:
  name: dashboard-service
spec:
  selector:
    app: devops-dashboard
  ports:
  - port: 80
    targetPort: 3000
  type: ClusterIP   # ← Change from NodePort to ClusterIP